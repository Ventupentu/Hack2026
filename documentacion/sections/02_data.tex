\section{Datos y Preprocesamiento}

\subsection{Datasets de Origen}

Las entradas principales son cuatro archivos CSV. Su rol funcional en el proyecto se resume en la Tabla~\ref{tab:datasets}.

\begin{table}[h]
  \centering
  \small
  \begin{tabular}{p{0.36\linewidth} p{0.56\linewidth}}
    \toprule
    \textbf{Archivo} & \textbf{Proposito} \\
    \midrule
    \texttt{data/bundles\_dataset.csv} & Catalogo de bundles y URLs de imagen de bundles. \\
    \texttt{data/product\_dataset.csv} & Catalogo de productos, URLs de imagen de productos y descripciones textuales. \\
    \texttt{data/bundles\_product\_match\_train.csv} & Enlaces supervisados bundle-producto para entrenamiento. \\
    \texttt{data/bundles\_product\_match\_test.csv} & Plantilla de test donde se debe predecir \texttt{product\_asset\_id}. \\
    \bottomrule
  \end{tabular}
  \caption{Datasets del repositorio usados por entrenamiento e inferencia.}
  \label{tab:datasets}
\end{table}

\subsection{Pipeline de Preprocesamiento}

El script \texttt{preprocess\_data.py} ofrece:
\begin{enumerate}
  \item Validaciones de esquema para columnas requeridas e integridad referencial.
  \item Limpieza de etiquetas de descripcion de producto, incluyendo exclusion de categorias predefinidas de cosmetica/perfumeria.
  \item Particion estratificada train/validacion por numero de etiquetas por bundle.
  \item Descarga concurrente de imagenes con logica de reintentos y registro de URLs prohibidas.
  \item Generacion de manifiestos JSONL y estadisticas de preprocesamiento.
\end{enumerate}

El script genera:
\begin{itemize}
  \item \texttt{manifests/train\_manifest.jsonl}
  \item \texttt{manifests/val\_manifest.jsonl}
  \item \texttt{labels.json}, \texttt{label2idx.json} y \texttt{stats.json}
\end{itemize}

\subsection{Aumento Offline}

El script \texttt{offline\_augment.py} genera vistas aumentadas deterministas, controladas por semilla, tanto para bundles como para productos. Aplica recortes redimensionados, jitter de color, flips horizontales opcionales, desenfoque gaussiano, cutout opcional (bundles) y perturbacion de calidad JPEG. Las imagenes aumentadas y los manifiestos se guardan como:
\begin{itemize}
  \item \texttt{bundles\_aug/*.jpg}, \texttt{bundles\_aug\_manifest.jsonl}
  \item \texttt{products\_aug/*.jpg}, \texttt{products\_aug\_manifest.jsonl}
\end{itemize}

Este diseno esta alineado con la restriccion de una sola imagen por producto, donde las representaciones robustas de producto requieren expansion sintetica de vistas.
